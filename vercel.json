{
  "buildCommand": "cd novacrm && npm run build",
  "outputDirectory": "novacrm/.next",
  "installCommand": "cd novacrm && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/novacrm/$1"
    }
  ]
}
